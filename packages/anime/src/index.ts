// Generated by scripts/build-platforms.mjs; edit src/index.ts and scripts/platforms.mjs.
import { Type } from "typebox";
import { defineToolPlugin } from "openclaw/plugin-sdk/tool-plugin";
import { callCrawlora } from "./client.js";
const plugin = defineToolPlugin({
 id: "crawlora-anime",
 name: "Anime by Crawlora",
 description: "Public Anime data through Crawlora.",
 configSchema: Type.Object({
    apiKey: Type.Optional(
      Type.String({
        description:
          "Crawlora API key. Falls back to the CRAWLORA_API_KEY environment variable."
      })
    )
  }),
 tools: (tool) => [tool({name:"crawlora_anime_character_search",description:"Use Crawlora to access public Anime data.",parameters:Type.Object({"query": Type.String({"description":"query parameter."}),"page": Type.Optional(Type.Integer({"description":"page parameter."})),"per_page": Type.Optional(Type.Integer({"description":"per_page parameter."}))}),async execute(params,config,context){return callCrawlora(config,context.signal,"anime-character-search",params as Record<string, unknown>);}})],
});
const register = plugin.register;
plugin.register = (api) => {
 const umbrella = api.config?.plugins?.entries?.crawlora;
 if (umbrella && umbrella.enabled !== false) {
  throw new Error("Disable the Crawlora starter plugin first (openclaw plugins disable crawlora). Its tools overlap with crawlora-anime.");
 }
 return register(api);
};
export default plugin;
