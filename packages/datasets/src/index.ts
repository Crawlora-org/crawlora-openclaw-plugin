// Generated by scripts/build-platforms.mjs; edit src/index.ts and scripts/platforms.mjs.
import { Type } from "typebox";
import { defineToolPlugin } from "openclaw/plugin-sdk/tool-plugin";
import { callCrawlora } from "./client.js";
const plugin = defineToolPlugin({
 id: "crawlora-datasets",
 name: "Datasets by Crawlora",
 description: "Public Datasets data through Crawlora.",
 configSchema: Type.Object({
    apiKey: Type.Optional(
      Type.String({
        description:
          "Crawlora API key. Falls back to the CRAWLORA_API_KEY environment variable."
      })
    )
  }),
 tools: (tool) => [tool({name:"crawlora_datasets_airbnb_markets_search",description:"Use Crawlora to access public Datasets data.",parameters:Type.Object({"group_by": Type.Optional(Type.Union([Type.Literal("country"),Type.Literal("market"),Type.Literal("admin1"),Type.Literal("locality"),Type.Literal("room_type"),Type.Literal("property_type")], {"description":"group_by parameter."})),"country": Type.Optional(Type.String({"description":"country parameter."})),"market": Type.Optional(Type.String({"description":"market parameter."})),"superhost": Type.Optional(Type.Boolean({"description":"superhost parameter."})),"guest_favorite": Type.Optional(Type.Boolean({"description":"guest_favorite parameter."})),"min_rating": Type.Optional(Type.Number({"description":"min_rating parameter."})),"min_review_count": Type.Optional(Type.Integer({"description":"min_review_count parameter."})),"active_since": Type.Optional(Type.String({"description":"active_since parameter."})),"min_listings": Type.Optional(Type.Integer({"description":"min_listings parameter."})),"sort": Type.Optional(Type.Union([Type.Literal("listings_desc"),Type.Literal("superhost_pct_desc"),Type.Literal("rating_desc"),Type.Literal("key_asc")], {"description":"sort parameter."})),"page": Type.Optional(Type.Integer({"description":"page parameter."})),"page_size": Type.Optional(Type.Integer({"description":"page_size parameter."}))}),async execute(params,config,context){return callCrawlora(config,context.signal,"datasets-airbnb-markets-search",params as Record<string, unknown>);}})],
});
const register = plugin.register;
plugin.register = (api) => {
 const umbrella = api.config?.plugins?.entries?.crawlora;
 if (umbrella && umbrella.enabled !== false) {
  throw new Error("Disable the Crawlora starter plugin first (openclaw plugins disable crawlora). Its tools overlap with crawlora-datasets.");
 }
 return register(api);
};
export default plugin;
