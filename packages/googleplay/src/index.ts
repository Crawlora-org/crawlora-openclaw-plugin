// Generated by scripts/build-platforms.mjs; edit src/index.ts and scripts/platforms.mjs.
import { Type } from "typebox";
import { defineToolPlugin } from "openclaw/plugin-sdk/tool-plugin";
import { callCrawlora } from "./client.js";
const plugin = defineToolPlugin({
 id: "crawlora-googleplay",
 name: "Googleplay by Crawlora",
 description: "Public Googleplay data through Crawlora.",
 configSchema: Type.Object({
    apiKey: Type.Optional(
      Type.String({
        description:
          "Crawlora API key. Falls back to the CRAWLORA_API_KEY environment variable."
      })
    )
  }),
 tools: (tool) => [tool({name:"crawlora_googleplay_app",description:"Use Crawlora to access public Googleplay data.",parameters:Type.Object({"app_id": Type.String({"description":"app_id parameter."}),"country": Type.Optional(Type.String({"description":"country parameter."})),"lang": Type.Optional(Type.String({"description":"lang parameter."}))}),async execute(params,config,context){return callCrawlora(config,context.signal,"googleplay-app",params as Record<string, unknown>);}})],
});
const register = plugin.register;
plugin.register = (api) => {
 const umbrella = api.config?.plugins?.entries?.crawlora;
 if (umbrella && umbrella.enabled !== false) {
  throw new Error("Disable the Crawlora starter plugin first (openclaw plugins disable crawlora). Its tools overlap with crawlora-googleplay.");
 }
 return register(api);
};
export default plugin;
